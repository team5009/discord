import { Command } from "types";

export default {
    name: 'folder_file',
    description: 'folder_file',
    legacy: true,
    execute: async (client, interaction, message, args) => {
        return 'folder_file'
    }
} as Command